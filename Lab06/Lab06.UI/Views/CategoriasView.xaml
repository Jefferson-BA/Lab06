<UserControl x:Class="Lab06.UI.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Nombre Categoría:"/>
                <TextBox Text="{Binding NombreCategoria, UpdateSourceTrigger=PropertyChanged}" Height="24" Margin="0,0,0,5"/>
                <TextBlock Text="Descripción:"/>
                <TextBox Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}" Height="45" TextWrapping="Wrap" AcceptsReturn="True"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Right">
            <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Width="80" Height="28" Margin="5" Background="#64748B" Foreground="White"/>
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="110" Height="28" Margin="5" Background="#2563EB" Foreground="White"/>
            <Button Content="Baja Lógica" Command="{Binding EliminarCommand}" Width="110" Height="28" Margin="5" Background="#DC2626" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding CategoriasView}" SelectedItem="{Binding SelectedCategoria}" AutoGenerateColumns="True" IsReadOnly="True" AlternatingRowBackground="#F1F5F9"/>
    </Grid>
</UserControl>
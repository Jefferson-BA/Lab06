<UserControl x:Class="Lab06.UI.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nombre Producto:"/>
                    <TextBox Text="{Binding NombreProducto, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                    <TextBlock Text="Cant. Por Unidad:" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding CantidadPorUnidad, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="ID Proveedor:"/>
                    <TextBox Text="{Binding ProveedorID, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                    <TextBlock Text="Precio Unidad:" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding PrecioUnidad, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="ID Categoría:"/>
                    <TextBox Text="{Binding CategoriaID, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                    <TextBlock Text="Stock Existencia:" Margin="0,5,0,0"/>
                    <TextBox Text="{Binding UnidadesEnExistencia, UpdateSourceTrigger=PropertyChanged}" Height="24"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Right">
            <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Width="80" Height="28" Margin="5" Background="#64748B" Foreground="White"/>
            <Button Content="Guardar / Actualizar" Command="{Binding GuardarCommand}" Width="140" Height="28" Margin="5" Background="#2563EB" Foreground="White"/>
            <Button Content="Baja Lógica (Activo=0)" Command="{Binding EliminarCommand}" Width="150" Height="28" Margin="5" Background="#DC2626" Foreground="White"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ProductosView}" SelectedItem="{Binding SelectedProducto}" AutoGenerateColumns="True" IsReadOnly="True" AlternatingRowBackground="#F1F5F9"/>
    </Grid>
</UserControl>